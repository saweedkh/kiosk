# Kiosk Desktop (Tauri)

Single Windows/macOS app: **full Next.js UI + full Django backend**.

```bash
npm install
npm run dev    # WebView + Django (sidecar or python fallback)
npm run build  # Requires Windows sidecar for release MSI
```

See [docs/TAURI_DESKTOP.md](../docs/TAURI_DESKTOP.md).
