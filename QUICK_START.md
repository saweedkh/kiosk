# راهنمای سریع تحویل به مشتری

## 🎯 خلاصه

برای تحویل پکیج به مشتری، فقط **2 دستور** نیاز دارید:

```bash
# 1. ساخت Docker Images
build-images.bat

# 2. ساخت پکیج نهایی
create-delivery-package.bat
```

بعد از این، فایل `kiosk-app.zip` آماده تحویل است!

---

## 📦 محتویات پکیج

پکیج شامل این فایل‌هاست:

- ✅ `docker-compose.yml` - تنظیمات Docker
- ✅ `run.bat` - راه‌اندازی برنامه
- ✅ `stop.bat` - توقف برنامه  
- ✅ `README.txt` - راهنمای فارسی مشتری
- ✅ `NETWORK_ACCESS.md` - راهنمای POS و Printer
- ✅ `images/` - Docker Images (3 فایل .tar)

**هیچ سورس کدی در پکیج نیست!**

---

## 🚀 مراحل تحویل

### مرحله 1: ساخت Images

```bash
build-images.bat
```

⏱️ زمان: 5-15 دقیقه (بسته به سرعت اینترنت و CPU)

✅ خروجی: پوشه `images/` با 3 فایل `.tar`

### مرحله 2: ساخت پکیج

```bash
create-delivery-package.bat
```

⏱️ زمان: 1-2 دقیقه

✅ خروجی: فایل `kiosk-app.zip`

### مرحله 3: تحویل

فایل `kiosk-app.zip` را به مشتری بدهید.

---

## 📋 چک‌لیست قبل از تحویل

- [ ] `build-images.bat` با موفقیت اجرا شد
- [ ] پوشه `images/` وجود دارد و شامل 3 فایل `.tar` است
- [ ] حجم فایل‌های `.tar` منطقی است (هر کدام چند صد مگابایت)
- [ ] `create-delivery-package.bat` با موفقیت اجرا شد
- [ ] فایل `kiosk-app.zip` ایجاد شده است
- [ ] حجم ZIP فایل منطقی است (2-5 GB)

---

## 🧪 تست پکیج (اختیاری اما توصیه می‌شود)

### تست سریع:

1. Extract کردن ZIP در یک پوشه جدید
2. بررسی وجود همه فایل‌ها
3. اجرای `run.bat` (اگر Docker نصب است)
4. بررسی `http://localhost`

---

## 📝 یادداشت برای مشتری

به مشتری بگویید:

1. **پیش‌نیاز:** Docker Desktop باید نصب باشد
2. **نصب:** Extract کردن ZIP
3. **راه‌اندازی:** دوبار کلیک روی `run.bat`
4. **دسترسی:** باز کردن `http://localhost`
5. **مشکل:** مطالعه `README.txt`

---

## ⚠️ نکات مهم

1. **حجم پکیج:** 2-5 GB (به دلیل Docker Images)
2. **زمان ساخت:** 10-20 دقیقه
3. **فقط Windows:** اما در WSL2 هم کار می‌کند
4. **پورت 80:** باید آزاد باشد

---

## 🔄 به‌روزرسانی

اگر نیاز به به‌روزرسانی دارید:

```bash
# 1. تغییرات در کد
# 2. ساخت Images جدید
build-images.bat

# 3. ساخت پکیج جدید
create-delivery-package.bat

# 4. تحویل ZIP جدید به مشتری
```

---

## ✅ آماده است!

بعد از اجرای 2 دستور بالا، فایل `kiosk-app.zip` آماده تحویل به مشتری است.

**هیچ کار دیگری نیاز نیست!** 🎉

